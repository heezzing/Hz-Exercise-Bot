# OpenRouter
OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxx

# PostgreSQL (PostGIS + pgvector 포함)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/workout_curation

# 앱 설정
APP_ENV=development
SECRET_KEY=change-me-to-random-secret
